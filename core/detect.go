package core

import (
	"fmt"
	"image"
	//"image/color"
)

//DetectFace
func DetectFace(imgsrc, imgmodel image.Image) image.Rectangle {
	fmt.Printf("+ Searching for face(s)...\n")
	return image.Rect(0, 0, 0, 0)
}
